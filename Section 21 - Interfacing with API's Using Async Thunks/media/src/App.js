import UserList from "./components/UserList"

function App(params) {
    return (<div className='container mx-auto'>
        <UserList />
    </div>)
}

export default App

// to run db.json => npm run start:server
// to run react => npm run start